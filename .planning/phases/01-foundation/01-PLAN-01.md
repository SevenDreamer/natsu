---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/Cargo.toml
  - src-tauri/tauri.conf.json
  - src-tauri/src/lib.rs
  - src-tauri/src/main.rs
  - src-tauri/src/commands/mod.rs
  - src-tauri/src/commands/storage.rs
  - src-tauri/src/db/mod.rs
  - src-tauri/src/db/schema.rs
  - src-tauri/src/models/mod.rs
  - src-tauri/src/models/settings.rs
  - package.json
  - vite.config.ts
  - tsconfig.json
  - tailwind.config.js
  - src/main.tsx
  - src/App.tsx
  - src/styles/globals.css
  - vitest.config.ts
  - tests/setup.ts
autonomous: true
requirements:
  - KNOW-04
must_haves:
  truths:
    - "User can select storage location on first launch"
    - "System creates raw/wiki/outputs directory structure"
    - "Storage path persists across app restarts"
    - "Basic UI renders with responsive layout"
    - "Test infrastructure is functional"
  artifacts:
    - path: "src-tauri/src/commands/storage.rs"
      provides: "Storage path selection and directory creation"
      contains: "select_storage_path"
    - path: "src-tauri/src/db/schema.rs"
      provides: "SQLite schema for app settings"
      contains: "CREATE TABLE"
    - path: "src/App.tsx"
      provides: "Root React component"
      min_lines: 20
    - path: "vitest.config.ts"
      provides: "Test configuration"
      contains: "defineConfig"
  key_links:
    - from: "src/App.tsx"
      to: "src-tauri/src/commands/storage.rs"
      via: "Tauri invoke"
      pattern: "invoke.*select_storage_path"
---

<objective>
Initialize Tauri 2.x project with React frontend, Rust backend, and establish storage foundation.

Purpose: Create the project skeleton with proper configuration for cross-platform development.
Output: Working Tauri app that can select and persist storage location with raw/wiki/outputs directories.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
<name>Task 1: Initialize Tauri 2.x project with React and TypeScript</name>
<read_first>
- CLAUDE.md (project tech stack decisions)
- .planning/phases/01-foundation/01-RESEARCH.md (Standard Stack section for versions)
</read_first>
<files>
package.json, src-tauri/Cargo.toml, src-tauri/tauri.conf.json, src-tauri/src/main.rs, src/main.tsx, src/App.tsx, index.html, vite.config.ts, tsconfig.json, tailwind.config.js, src/styles/globals.css
</files>
<action>
Initialize the Tauri 2.x project using the official CLI. This project is NEW (no existing code).

1. Run: `npm create tauri-app@latest termsuite -- --template react-ts --manager npm`
2. Navigate to project: `cd termsuite`
3. Install frontend dependencies per RESEARCH.md Standard Stack:
   - `npm install zustand react-router-dom lucide-react`
   - `npm install -D tailwindcss @tailwindcss/vite`
   - `npm install -D vitest @testing-library/react @testing-library/jest-dom @vitest/coverage-v8 jsdom`
4. Configure Tailwind CSS 4.x (per RESEARCH.md):
   - Create `tailwind.config.js` with content paths for `./src/**/*.{js,ts,jsx,tsx}`
   - Update `vite.config.ts` to include `@tailwindcss/vite()` plugin
   - Create `src/styles/globals.css` with `@import "tailwindcss";`
5. Configure Tauri permissions in `src-tauri/capabilities/default.json`:
   - Add `fs:default`, `dialog:default`, `shell:default` permissions
6. Update `src-tauri/tauri.conf.json`:
   - Set `productName: "TermSuite"`
   - Set `version: "0.1.0"`
   - Configure `frontendDist: "../dist"` and `devUrl: "http://localhost:5173"`
7. Create basic App.tsx with a placeholder "TermSuite" heading

Note: Do NOT initialize shadcn/ui yet (that's in PLAN-02). This task focuses on raw project structure.
</action>
<acceptance_criteria>
- `package.json` exists with `@tauri-apps/api`, `@tauri-apps/cli`, `react`, `react-dom`, `typescript`, `vite`, `tailwindcss`, `zustand`, `vitest` in dependencies
- `src-tauri/Cargo.toml` exists with `tauri`, `tauri-plugin-fs`, `tauri-plugin-dialog`, `serde`, `rusqlite` in dependencies
- `npm run dev` starts the Tauri dev server without errors
- `npm test` runs vitest (even with 0 tests passing)
- `src/App.tsx` contains `TermSuite` text
</acceptance_criteria>
<verify>
<automated>bash -c "cd /data/data/com.termux/files/home/projects/term/termsuite && test -f package.json && test -f src-tauri/Cargo.toml && grep -q 'tauri' src-tauri/Cargo.toml && grep -q 'zustand' package.json && grep -q 'vitest' package.json && echo 'PASS'"</automated>
</verify>
<done>Project initialized with Tauri 2.x, React, TypeScript, Tailwind CSS 4.x, and test infrastructure configured.</done>
</task>

<task type="auto">
<name>Task 2: Create Rust backend module structure and storage commands</name>
<read_first>
- .planning/phases/01-foundation/01-RESEARCH.md (Pattern 1: Tauri Command Pattern)
- .planning/phases/01-foundation/01-CONTEXT.md (D-06, D-07, D-08 storage decisions)
</read_first>
<files>
src-tauri/src/lib.rs, src-tauri/src/commands/mod.rs, src-tauri/src/commands/storage.rs, src-tauri/src/db/mod.rs, src-tauri/src/db/schema.rs, src-tauri/src/models/mod.rs, src-tauri/src/models/settings.rs
</files>
<action>
Create the Rust backend module structure with storage-related commands.

1. Create module files:
   - `src-tauri/src/commands/mod.rs` - exports storage module
   - `src-tauri/src/commands/storage.rs` - storage path selection and directory creation
   - `src-tauri/src/db/mod.rs` - database connection management
   - `src-tauri/src/db/schema.rs` - SQLite schema for settings
   - `src-tauri/src/models/mod.rs` - exports settings model
   - `src-tauri/src/models/settings.rs` - Settings struct

2. Implement storage.rs with these Tauri commands:
   - `select_storage_path()` - Opens native directory picker dialog (using tauri-plugin-dialog)
   - `init_storage(path: String)` - Creates raw/, wiki/, outputs/ directories at selected path
   - `get_storage_path()` - Returns persisted storage path from settings DB
   - `set_storage_path(path: String)` - Persists storage path to settings DB

3. Implement db/schema.rs with:
   ```sql
   CREATE TABLE IF NOT EXISTS settings (
       key TEXT PRIMARY KEY,
       value TEXT NOT NULL
   );
   ```

4. Update lib.rs to:
   - Import all modules
   - Register tauri plugins: fs, dialog, shell
   - Manage Database state (Mutex<Connection>)
   - Register invoke_handler with storage commands

Per D-06: Storage location selected by user at first launch.
Per D-07: User can change storage location in settings.
Per D-08: raw/wiki/outputs directories created under user-selected root.
</action>
<acceptance_criteria>
- `src-tauri/src/commands/storage.rs` contains `pub async fn select_storage_path()`
- `src-tauri/src/commands/storage.rs` contains `pub async fn init_storage(path: String)`
- `src-tauri/src/db/schema.rs` contains `CREATE TABLE IF NOT EXISTS settings`
- `src-tauri/src/lib.rs` contains `.invoke_handler(tauri::generate_handler![...])`
- `cargo build --manifest-path src-tauri/Cargo.toml` compiles without errors
</acceptance_criteria>
<verify>
<automated>bash -c "cd /data/data/com.termux/files/home/projects/term/termsuite && grep -q 'select_storage_path' src-tauri/src/commands/storage.rs && grep -q 'init_storage' src-tauri/src/commands/storage.rs && grep -q 'CREATE TABLE IF NOT EXISTS settings' src-tauri/src/db/schema.rs && cargo check --manifest-path src-tauri/Cargo.toml 2>&1 | tail -5"</automated>
</verify>
<done>Backend module structure created with storage commands for path selection and directory initialization.</done>
</task>

<task type="auto">
<name>Task 3: Create test infrastructure and Wave 0 tests</name>
<read_first>
- .planning/phases/01-foundation/01-VALIDATION.md (Wave 0 Requirements)
- .planning/phases/01-foundation/01-RESEARCH.md (Validation Architecture section)
</read_first>
<files>
vitest.config.ts, tests/setup.ts, tests/e2e/storage.test.ts, tests/e2e/dirs.test.ts, tests/settings/storage.test.ts
</files>
<action>
Set up Vitest test infrastructure and create Wave 0 test scaffolds.

1. Create `vitest.config.ts`:
   ```typescript
   import { defineConfig } from 'vitest/config'
   import react from '@vitejs/plugin-react'

   export default defineConfig({
     plugins: [react()],
     test: {
       environment: 'jsdom',
       setupFiles: ['./tests/setup.ts'],
       include: ['tests/**/*.test.ts', 'tests/**/*.test.tsx'],
     },
   })
   ```

2. Create `tests/setup.ts`:
   ```typescript
   import '@testing-library/jest-dom'
   ```

3. Create test file scaffolds for KNOW-04 coverage:
   - `tests/e2e/storage.test.ts` - Test storage path selection and persistence
   - `tests/e2e/dirs.test.ts` - Test raw/wiki/outputs directory creation
   - `tests/settings/storage.test.ts` - Test storage settings persistence

4. Add test scripts to package.json:
   - `"test": "vitest run"`
   - `"test:watch": "vitest"`
   - `"test:coverage": "vitest run --coverage"`

Each test file should have at least one placeholder test that will be implemented in later plans.
</action>
<acceptance_criteria>
- `vitest.config.ts` exists with `defineConfig` export
- `tests/setup.ts` exists with `@testing-library/jest-dom` import
- `tests/e2e/storage.test.ts` exists with at least one `test()` or `describe()` block
- `tests/e2e/dirs.test.ts` exists with at least one `test()` or `describe()` block
- `tests/settings/storage.test.ts` exists with at least one `test()` or `describe()` block
- `npm test` runs without config errors (tests may be skipped/placeholder)
</acceptance_criteria>
<verify>
<automated>bash -c "cd /data/data/com.termux/files/home/projects/term/termsuite && test -f vitest.config.ts && test -f tests/setup.ts && test -f tests/e2e/storage.test.ts && test -f tests/e2e/dirs.test.ts && test -f tests/settings/storage.test.ts && echo 'PASS'"</automated>
</verify>
<done>Test infrastructure configured with Wave 0 test scaffolds in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend → Tauri Backend | All Tauri invoke calls cross this boundary |
| Tauri Backend → File System | File operations must validate paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | storage.rs:init_storage | mitigate | Validate path is within allowed storage root, reject path traversal (../) |
| T-01-02 | Tampering | storage.rs:select_storage_path | accept | Uses native OS dialog, user explicitly chooses location |
</threat_model>

<verification>
- [ ] `npm run dev` launches Tauri app without errors
- [ ] `cargo build` in src-tauri compiles successfully
- [ ] `npm test` runs test suite
- [ ] Storage commands are registered and callable from frontend
</verification>

<success_criteria>
1. Project structure follows RESEARCH.md recommendations
2. Rust backend compiles with storage commands exposed
3. Test infrastructure is functional
4. Storage path selection flow is ready for UI integration (PLAN-02)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-PLAN-01-SUMMARY.md`
</output>
