---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/button.tsx
  - src/components/ui/input.tsx
  - src/components/ui/dialog.tsx
  - src/components/ui/scroll-area.tsx
  - src/components/ui/separator.tsx
  - src/components/ui/tooltip.tsx
  - components.json
  - src/lib/utils.ts
  - src/components/layout/AppLayout.tsx
  - src/components/layout/Sidebar.tsx
  - src/components/layout/MainPanel.tsx
  - src/components/layout/PreviewPanel.tsx
  - src/components/layout/MobileDrawer.tsx
  - src/stores/uiStore.ts
  - src/App.tsx
autonomous: true
requirements:
  - KNOW-04
must_haves:
  truths:
    - "User sees responsive three-column layout on desktop"
    - "User sees chat-style layout with drawer on mobile"
    - "Layout switches automatically at 768px breakpoint"
    - "shadcn/ui components are initialized and functional"
  artifacts:
    - path: "src/components/layout/AppLayout.tsx"
      provides: "Root responsive layout component"
      min_lines: 50
    - path: "src/components/ui/button.tsx"
      provides: "shadcn Button component"
      contains: "export function Button"
    - path: "components.json"
      provides: "shadcn configuration"
      contains: "style"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/layout/AppLayout.tsx"
      via: "React import"
      pattern: "import.*AppLayout"
</objective>

<objective>
Initialize shadcn/ui component library and create responsive three-column layout.

Purpose: Establish the UI foundation with shadcn/ui components and responsive layout per UI-SPEC.md.
Output: Working responsive layout with collapsible sidebars for desktop and drawer for mobile.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
<name>Task 1: Initialize shadcn/ui with Tailwind CSS 4.x configuration</name>
<read_first>
- .planning/phases/01-foundation/01-UI-SPEC.md (Design System section)
- .planning/phases/01-foundation/01-RESEARCH.md (UI Components section)
- CLAUDE.md (GitHub-style minimalist design direction)
</read_first>
<files>
components.json, src/lib/utils.ts, tailwind.config.js, src/styles/globals.css, src/components/ui/button.tsx, src/components/ui/input.tsx, src/components/ui/dialog.tsx, src/components/ui/scroll-area.tsx, src/components/ui/separator.tsx, src/components/ui/tooltip.tsx
</files>
<action>
Initialize shadcn/ui with Tailwind CSS 4.x and add required components.

1. Run shadcn initialization:
   ```bash
   npx shadcn init -d
   ```
   This creates `components.json` with default configuration.

2. Add required components from UI-SPEC.md Component Inventory:
   ```bash
   npx shadcn add button input dialog scroll-area separator tooltip -y
   ```

3. Verify Tailwind CSS 4.x configuration in `tailwind.config.js`:
   - Content paths: `["./index.html", "./src/**/*.{js,ts,jsx,tsx}"]`
   - Theme includes GitHub-style colors per UI-SPEC.md

4. Ensure `src/lib/utils.ts` contains the `cn` utility function (shadcn creates this).

5. Update `src/styles/globals.css` with:
   - Tailwind import: `@import "tailwindcss";`
   - CSS variables for light/dark themes per UI-SPEC.md Color section:
     - Light: Dominant #FFFFFF, Secondary #F6F8FA, Accent #0969DA, Destructive #CF222E
     - Dark: Dominant #0D1117, Secondary #161B22, Accent #58A6FF, Destructive #F85149

Per UI-SPEC.md: Font is Inter (GitHub-style sans-serif).
</action>
<acceptance_criteria>
- `components.json` exists with `style` field
- `src/lib/utils.ts` exists with `cn` function export
- `src/components/ui/button.tsx` exists with `export function Button`
- `src/components/ui/input.tsx` exists with `export function Input`
- `src/components/ui/dialog.tsx` exists with dialog exports
- `src/styles/globals.css` contains CSS variables for `--background`, `--foreground`, `--primary`
</acceptance_criteria>
<verify>
<automated>bash -c "cd /data/data/com.termux/files/home/projects/term/termsuite && test -f components.json && test -f src/lib/utils.ts && test -f src/components/ui/button.tsx && grep -q 'export function Button' src/components/ui/button.tsx && echo 'PASS'"</automated>
</verify>
<done>shadcn/ui initialized with Tailwind CSS 4.x and all Phase 1 UI components installed.</done>
</task>

<task type="auto">
<name>Task 2: Create responsive AppLayout with three-column desktop and mobile drawer</name>
<read_first>
- .planning/phases/01-foundation/01-UI-SPEC.md (Layout Contract section)
- .planning/phases/01-foundation/01-CONTEXT.md (D-03, D-04, D-05 layout decisions)
- .planning/phases/01-foundation/01-RESEARCH.md (Pattern 6: Responsive Layout)
</read_first>
<files>
src/components/layout/AppLayout.tsx, src/components/layout/Sidebar.tsx, src/components/layout/MainPanel.tsx, src/components/layout/PreviewPanel.tsx, src/components/layout/MobileDrawer.tsx, src/stores/uiStore.ts, src/App.tsx
</files>
<action>
Create responsive layout components following UI-SPEC.md Layout Contract.

1. Create `src/stores/uiStore.ts` with Zustand store:
   ```typescript
   interface UIState {
     isMobile: boolean;
     sidebarOpen: boolean;
     previewOpen: boolean;
     drawerOpen: boolean;
     setMobile: (isMobile: boolean) => void;
     toggleSidebar: () => void;
     togglePreview: () => void;
     toggleDrawer: () => void;
   }
   ```

2. Create `src/components/layout/AppLayout.tsx`:
   - Use `MOBILE_BREAKPOINT = 768` constant
   - `useEffect` with resize listener to detect mobile/desktop
   - Desktop: Three-column layout with `<Sidebar>`, `<MainPanel>`, `<PreviewPanel>`
   - Mobile: Single column with header hamburger menu, `<MobileDrawer>`
   - Apply Tailwind classes for layout: `h-screen flex`, `w-60`, `w-80`, transition classes
   - Per D-03: Sidebar 240px (collapsible to 48px), Preview 320px (collapsible)
   - Per D-04: Mobile uses chat-style layout with drawer

3. Create `src/components/layout/Sidebar.tsx`:
   - Accept `collapsed: boolean` and `onToggle: () => void` props
   - Use `w-60` when open, `w-12` when collapsed
   - Include placeholder for file tree (implemented in PLAN-04)
   - Toggle button using shadcn Button with `Menu` icon from lucide-react

4. Create `src/components/layout/MainPanel.tsx`:
   - Flexible center area (`flex-1 min-w-0`)
   - Placeholder content "Select or create a note"

5. Create `src/components/layout/PreviewPanel.tsx`:
   - Accept `collapsed: boolean` and `onToggle: () => void` props
   - Right sidebar for preview/metadata

6. Create `src/components/layout/MobileDrawer.tsx`:
   - Use shadcn Dialog or CSS transforms for slide-in drawer
   - `open: boolean` and `onClose: () => void` props
   - Full-height drawer from left side

7. Update `src/App.tsx` to use `<AppLayout />` as root component.
</action>
<acceptance_criteria>
- `src/components/layout/AppLayout.tsx` exists with responsive breakpoint logic
- `src/stores/uiStore.ts` exists with sidebarOpen, previewOpen, drawerOpen state
- `src/App.tsx` imports and renders `<AppLayout />`
- Layout shows three columns on viewport >= 768px
- Layout shows single column with drawer trigger on viewport < 768px
- Sidebar collapses from 240px to 48px
- Preview panel collapses from 320px to 0
</acceptance_criteria>
<verify>
<automated>bash -c "cd /data/data/com.termux/files/home/projects/term/termsuite && grep -q 'MOBILE_BREAKPOINT\\|768' src/components/layout/AppLayout.tsx && grep -q 'sidebarOpen\\|previewOpen' src/stores/uiStore.ts && grep -q 'AppLayout' src/App.tsx && echo 'PASS'"</automated>
</verify>
<done>Responsive layout implemented with three-column desktop and mobile drawer patterns.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User Interaction → UI State | All user actions flow through Zustand store |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Tampering | UI components | accept | Client-side only, no security-sensitive data in Phase 1 |
</threat_model>

<verification>
- [ ] `npm run dev` renders AppLayout without errors
- [ ] Desktop layout shows three columns
- [ ] Mobile layout shows drawer trigger
- [ ] Sidebar toggle collapses/expands correctly
- [ ] Preview toggle shows/hides correctly
</verification>

<success_criteria>
1. shadcn/ui components render correctly
2. Layout is responsive per UI-SPEC.md specifications
3. State management via Zustand works
4. Both desktop and mobile layouts are functional
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-PLAN-02-SUMMARY.md`
</output>
