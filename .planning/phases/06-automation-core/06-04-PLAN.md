---
id: "06-04"
phase: 6
name: "API Calls"
wave: 1
autonomous: true
objective: "实现 HTTP API 调用功能，支持配置化请求和请求历史记录"
requirements: ["AUTO-04"]
files_modified:
  - "natsu/src-tauri/src/db/schema.rs"
  - "natsu/src-tauri/src/commands/mod.rs"
  - "natsu/src-tauri/src/commands/api.rs"
  - "natsu/src-tauri/src/main.rs"
  - "natsu/src/components/automation/ApiCalls.tsx"
  - "natsu/src/components/automation/ApiConfigEditor.tsx"
  - "natsu/src/stores/automationStore.ts"
  - "natsu/src/lib/tauri.ts"
key_files:
  created:
    - "natsu/src-tauri/src/commands/api.rs"
    - "natsu/src/components/automation/ApiCalls.tsx"
    - "natsu/src/components/automation/ApiConfigEditor.tsx"
  modified:
    - "natsu/src-tauri/src/db/schema.rs"
    - "natsu/src/stores/automationStore.ts"
---

# Plan 06-04: API Calls

**Phase:** 6 - Automation Core
**Requirement:** AUTO-04 - 用户可以从应用程序进行 HTTP API 调用

## Objective

实现 HTTP API 调用功能，支持配置化请求、模板变量、认证配置和请求历史记录。复用已有的 reqwest 依赖。

## Scope

### In Scope
- API 配置数据库表
- HTTP 请求执行（GET/POST/PUT/DELETE）
- 请求/响应历史记录
- 认证配置（Basic, Bearer, API Key）
- 模板变量替换

### Out of Scope
- GraphQL 支持
- WebSocket
- 导入 Postman/Insomnia 集合
- 请求链（chaining）

## Tasks

### Task 1: 数据库 Schema

**File:** `natsu/src-tauri/src/db/schema.rs`

```sql
CREATE TABLE IF NOT EXISTS api_configs (
    id TEXT PRIMARY KEY,
    name TEXT NOT NULL,
    method TEXT NOT NULL,
    url TEXT NOT NULL,
    headers TEXT,
    body_template TEXT,
    auth_type TEXT DEFAULT 'none',
    auth_config TEXT,
    timeout_secs INTEGER DEFAULT 30,
    created_at INTEGER NOT NULL,
    updated_at INTEGER NOT NULL
);

CREATE TABLE IF NOT EXISTS api_history (
    id TEXT PRIMARY KEY,
    config_id TEXT,
    url TEXT NOT NULL,
    method TEXT NOT NULL,
    request_headers TEXT,
    request_body TEXT,
    response_status INTEGER,
    response_headers TEXT,
    response_body TEXT,
    duration_ms INTEGER,
    error TEXT,
    executed_at INTEGER NOT NULL,
    FOREIGN KEY (config_id) REFERENCES api_configs(id)
);

CREATE INDEX IF NOT EXISTS idx_api_history_time ON api_history(executed_at DESC);
```

### Task 2: API 模型

**File:** `natsu/src-tauri/src/models/api.rs` (新建)

```rust
use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ApiConfig {
    pub id: String,
    pub name: String,
    pub method: String,
    pub url: String,
    pub headers: Option<HashMap<String, String>>,
    pub body_template: Option<String>,
    pub auth_type: AuthType,
    pub auth_config: Option<AuthConfig>,
    pub timeout_secs: u64,
    pub created_at: i64,
    pub updated_at: i64,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub enum AuthType {
    None,
    Basic,
    Bearer,
    ApiKey,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct AuthConfig {
    pub username: Option<String>,
    pub password: Option<String>,
    pub token: Option<String>,
    pub key_name: Option<String>,
    pub key_value: Option<String>,
    pub key_location: Option<String>, // "header" or "query"
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ApiHistoryEntry {
    pub id: String,
    pub config_id: Option<String>,
    pub url: String,
    pub method: String,
    pub request_headers: Option<String>,
    pub request_body: Option<String>,
    pub response_status: Option<u16>,
    pub response_headers: Option<String>,
    pub response_body: Option<String>,
    pub duration_ms: Option<i64>,
    pub error: Option<String>,
    pub executed_at: i64,
}
```

### Task 3: 后端 API 实现

**File:** `natsu/src-tauri/src/commands/api.rs` (新建)

```rust
#[tauri::command]
pub fn list_api_configs(conn: State<Connection>) -> Result<Vec<ApiConfig>, String>

#[tauri::command]
pub fn get_api_config(id: String, conn: State<Connection>) -> Result<ApiConfig, String>

#[tauri::command]
pub fn create_api_config(
    config: NewApiConfig,
    conn: State<Connection>,
) -> Result<ApiConfig, String>

#[tauri::command]
pub fn update_api_config(
    id: String,
    config: UpdateApiConfig,
    conn: State<Connection>,
) -> Result<ApiConfig, String>

#[tauri::command]
pub fn delete_api_config(id: String, conn: State<Connection>) -> Result<(), String>

#[tauri::command]
pub async fn execute_api_request(
    config_id: Option<String>,
    url: String,
    method: String,
    headers: Option<HashMap<String, String>>,
    body: Option<String>,
    auth: Option<AuthConfig>,
    timeout_secs: Option<u64>,
    variables: Option<HashMap<String, String>>,
    conn: State<Connection>,
) -> Result<ApiResponse, String>

#[tauri::command]
pub fn get_api_history(
    config_id: Option<String>,
    limit: Option<usize>,
    conn: State<Connection>,
) -> Result<Vec<ApiHistoryEntry>, String>

#[tauri::command]
pub fn delete_api_history(id: String, conn: State<Connection>) -> Result<(), String>
```

### Task 4: 模板变量替换

支持的内置变量：
- `{{timestamp}}` - 当前时间戳
- `{{date}}` - 当前日期 (YYYY-MM-DD)
- `{{datetime}}` - 当前日期时间
- `{{uuid}}` - UUID v4
- `{{random}}` - 随机数

自定义变量：
- 通过 `variables` 参数传入

实现函数：
```rust
fn replace_variables(template: &str, variables: &HashMap<String, String>) -> String {
    let mut result = template.to_string();
    
    // Built-in variables
    result = result.replace("{{timestamp}}", &chrono::Utc::now().timestamp().to_string());
    result = result.replace("{{date}}", &chrono::Utc::now().format("%Y-%m-%d").to_string());
    result = result.replace("{{datetime}}", &chrono::Utc::now().to_rfc3339());
    result = result.replace("{{uuid}}", &uuid::Uuid::new_v4().to_string());
    result = result.replace("{{random}}", &rand::random::<u32>().to_string());
    
    // Custom variables
    for (key, value) in variables {
        result = result.replace(&format!("{{{{{}}}}}", key), value);
    }
    
    result
}
```

### Task 5: 前端组件

**File:** `natsu/src/components/automation/ApiCalls.tsx` (新建)

功能：
- API 配置列表
- 快速执行（点击发送）
- 历史记录查看
- 搜索/过滤

**File:** `natsu/src/components/automation/ApiConfigEditor.tsx` (新建)

配置表单：
- 请求名称
- HTTP 方法选择 (GET/POST/PUT/DELETE)
- URL 输入（支持变量提示）
- Headers 编辑器（键值对）
- Body 编辑器（JSON/Text）
- 认证配置
  - None
  - Basic Auth (username/password)
  - Bearer Token
  - API Key (header/query)
- 超时设置
- 变量预览

### Task 6: 响应展示

响应面板显示：
- 状态码（颜色区分）
- 响应时间
- 响应头
- 响应体（格式化 JSON）
- 复制响应按钮
- 保存到历史

## HTTP Methods

| Method | Description | Body Support |
|--------|-------------|--------------|
| GET | 获取资源 | 否 |
| POST | 创建资源 | 是 |
| PUT | 更新资源 | 是 |
| DELETE | 删除资源 | 否 |
| PATCH | 部分更新 | 是 |

## Verification

### Unit Tests
- [ ] API 配置 CRUD
- [ ] 变量替换逻辑
- [ ] 认证配置

### Integration Tests
- [ ] GET 请求执行
- [ ] POST 请求执行
- [ ] 认证头正确添加
- [ ] 错误处理（超时、网络错误）
- [ ] 历史记录保存

### Manual Tests
- [ ] 创建和执行各种 API 配置
- [ ] 模板变量替换
- [ ] 响应格式化和展示
- [ ] 历史记录查看

## Dependencies

- **无前置依赖**（Wave 1）
- **复用现有**：reqwest, tokio

## Success Criteria

1. ✅ 用户可以创建 API 请求配置
2. ✅ 用户可以执行 HTTP 请求
3. ✅ 用户可以查看响应（状态码、头、体）
4. ✅ 用户可以配置认证（Basic, Bearer, API Key）
5. ✅ 用户可以使用模板变量
6. ✅ 用户可以查看请求历史
